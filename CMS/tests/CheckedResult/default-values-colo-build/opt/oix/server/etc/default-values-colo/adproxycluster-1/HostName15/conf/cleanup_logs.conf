

package cleanup;
use strict;

our %SERVICE_CONFIG = (
  pid_file => 'cleanup_logs.pid',
  check_period => 600, # in seconds
);

# 'time', in minutes.
# 'size', in bytes.


our %CLEANUP_CONFIG = (
      
);

1;

